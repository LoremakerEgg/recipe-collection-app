// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("postgresql://postgres:IHEUEX4pMBsFnyd9@db.ptqgqeiujpwbziabrzux.supabase.co:5432/postgres")
}

model AllRecipes {
  id  Int   @id @unique
  recipeTitle String 
  recipeImageURL String
  cookingTime Int
  servings Int
  randomId Int @default(autoincrement())
}

model CookingInstructions{
  instructionId Int @id @unique
  instructions String
  recipe AllRecipes @relation(fields: [recipeId], references: [id])
  recipeId  Int
}

model Ingredients{
  ingredientId Int @id @unique
  recipe AllRecipes @relations(fields: [recipeId], references: [id])
  recipeId  Int
  ingredient string
  amount string
}